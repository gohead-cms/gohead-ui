import Card from '@mui/material/Card';
import { Title } from 'react-admin';

export const Dashboard = () => (
    <Card>
        <Title title="GoHead Dashboard" />
    </Card>
);

export default Dashboard;